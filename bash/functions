# Shared functions
[[ -e $DOTFILES/sh/functions ]] && source $DOTFILES/sh/functions || true

# Bash-specific functions

add-alias() {
  local name=$1 value="$2"
  echo "alias $name='$value'" >> ~/.dotfiles/companygardener/bash/aliases
  eval "alias $name='$value'"
  alias $name
}
